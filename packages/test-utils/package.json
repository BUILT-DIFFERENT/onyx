{
  "name": "@onyx/test-utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@onyx/validation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "lint:quick": "oxlint .",
    "lint:fix": "eslint . --fix",
    "test": "echo 'ERROR: No tests implemented for this package yet' && exit 1",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  }
}
